- the scoin scala bitcoin library wrapped in javascript clothes

to publish this JS package, run:

    yarn
    cd .. && sbt 'scoinJS/fastLinkJS'
    node build.js
    npm publish

or `just build`, `just bundle` and `just publish`
